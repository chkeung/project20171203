<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
    <meta charset="utf-8">
    <title>ss</title>
    <style>
      /* Always set the map height explicitly to define the size of the div
       * element that contains the map. */
      #map {
        height: 100%;
      }
      /* Optional: Makes the sample page fill the window. */
      html, body {
        height: 100%;
        margin: 0;
        padding: 0;
      }
    </style>
    <link rel="stylesheet" href="http://www.w3schools.com/lib/w3.css">
  </head>
  <body>
    <script>
      function goBack() {window.history.back();}
    </script>
    
    
      <div class="w3-container w3-teal">
      <h1><%= details.name %></h1>
      </div>
      <% if(details.photo != "Not offered"){ %>
        <% if(details.photoMimetype.includes("image")){ %>
      <img src="data: <%= details.photoMimetype %>; base64, <%= details.photo %>" class="w3-image w3-border w3-padding" style="width:100%">
      <% }} %>

      <div class="w3-container">
      <p>
      <label class="w3-label w3-validate">Borough: </label><%= details.borough %></p>
      <label class="w3-label w3-validate">Cuisine: </label><%= details.cuisine %></p>
      <label class="w3-label w3-validate">Street:  </label><%= details.address.street %></p>
      <label class="w3-label w3-validate">Building: </label><%= details.address.building %></p>
      <label class="w3-label w3-validate">Zipcode: </label><%= details.address.zipcode %></p>
      <label class="w3-label w3-validate">GPS: </label>[<%= details.address.coord[0] %>,<%= details.address.coord[1] %>]</p>
      <label class="w3-label w3-validate">Rating: </label></p>
      <p></p>
      <ol>
        <% for(var i=0; i< details.grades.length; i++){%>
          <li><%=details.grades[i].score%> (<%=details.grades[i].user%>) </li>
        <%}%>
      </ol>

      <p>
      <label class="w3-label w3-validate">Created by: </label><%= details.owner %></p>
      <% if(details.address.coord[0] != "Not offered" || details.address.coord[1] != "Not offered"){ %>
      <p><a href="/gmap?lat=<%= details.address.coord[0] %>&lon=<%= details.address.coord[1] %>&title=<%= details.name %>">Google Map</a></p>
      <% } %>
      </div>

      <div class="w3-container">
        <a href="/rate?_id=<%= details._id%>" style="text-decoration: none">
        <button class="w3-btn w3-section w3-teal w3-ripple">Rate</button>
        </a>
        <a href="/change?_id=<%= details._id%>" style="text-decoration: none">
        <button class="w3-btn w3-section w3-teal w3-ripple">Edit</button>
        </a>
        <a href="/remove?_id=<%= details._id%>" style="text-decoration: none">
        <button class="w3-btn w3-section w3-teal w3-ripple">Delete</button>
        </a>
        <button class="w3-btn w3-section w3-teal w3-ripple" onclick="goBack()">Go Back</button>
      </div>
      
  </body>
</html>
